<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Current-Weather</title>
    <link rel="stylesheet" href="styles.css" />
</head>
<body>
    <div class="container">
        <div class="header">
            <% if(locals.error){ %>
                <h2><%= error %></h2>
            <% }else if(!locals.city){ %>
                <h2>Enter your city/town name:</h2>
            <% }else{ %>
                <h2>the current temperature in <%= city %>: <%= temperature %></h2>
            <% } %>
        </div>

        <div class="form">
            <form action="/" id="myForm" method="POST">
                <input type="text" id="input" name="input" required> 
                <input type="submit" value="enter">
            </form>
        </div>
    </div>
</body>
</html>